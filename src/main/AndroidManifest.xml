<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.main">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Rules"></activity>
        <activity android:name=".TableSevenOnSevenActivity" />
        <activity android:name=".TableSixOnSixActivity" />
        <activity android:name=".TableFiveOnFiveActivity" />
        <activity android:name=".TableFourOnFourActivity" />
        <activity android:name=".TableMenu" />
        <activity android:name=".Monsters3" />
        <activity android:name=".Monsters2" />
        <activity android:name=".Monsters" />
        <activity android:name=".MonstersModeMenu" />
        <activity android:name=".Mosaic" />
        <activity android:name=".GamesMenu" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>